source 'https://rubygems.org'

gem 'rails', '3.2.8'

# Bundle edge Rails instead:
# gem 'rails', :git => 'git://github.com/rails/rails.git'

gem 'sqlite3'

gem 'blacklight', '3.6'
#gem 'hydra-head', 'v5.0.0.pre4' 
gem 'hydra-head', :git => 'git://github.com/projecthydra/hydra-head.git', :tag => 'v5.0.0.pre4'

gem 'devise'

# Gems used only for assets and not required
# in production environments by default.
group :assets do
  gem 'sass-rails',   '~> 3.2.3'
  gem 'coffee-rails', '~> 3.2.1'

  # See https://github.com/sstephenson/execjs#readme for more supported runtimes
  # gem 'therubyracer', :platforms => :ruby

  gem 'uglifier', '>= 1.0.3'
end

gem 'jquery-rails'

 # You will probably want to use these to run the tests you write for your hydra head
  # For testing with Cucumber
  group :cucumber do
    gem 'cucumber'
    gem 'cucumber-rails'
  end

  # For testing with rspec
  group :development, :test do
  	gem 'debugger'
    gem 'rspec-rails', '>=2.9.0'
    gem 'jettywrapper'
    gem 'database_cleaner'
  end

 #therubyracer Javacript runtime needed by the asset pipeline
gem 'therubyracer'

# To use ActiveModel has_secure_password
# gem 'bcrypt-ruby', '~> 3.0.0'

# To use Jbuilder templates for JSON
# gem 'jbuilder'

# Use unicorn as the app server
# gem 'unicorn'

# Deploy with Capistrano
# gem 'capistrano'

# To use debugger
# gem 'debugger'

gem 'active-fedora'
gem 'om'
gem 'rubytree'

gem "devise"
gem "compass-rails", "~> 1.0.0", :group => :assets
gem "compass-susy-plugin", "~> 0.9.0", :group => :assets